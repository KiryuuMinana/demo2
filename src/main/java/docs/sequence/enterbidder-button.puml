@startuml
start
:开标结束，推送入围/候补/可递补标记给评标;

:评标系统在未知时机发送递补指令;
:开标后端接收指令;

:新建一轮递补;
if (是否为首次递补?) then (是)
    :轮次=1;
    :初始化可选人数 = 候补初始人数 - 已候补人数;
else (否)
    :轮次 = 已递补轮次数 + 1;
    :初始化可选人数 = 上一次递补初始人数 - 上一次已递补人数;
endif
:轮次状态 = "0-进行中";

:向前端推送WebSocket消息;
:前端弹窗，展示递补列表;

:开标代理勾选单位;
if (勾选人数 ≥ 1?) then (是)
    :校验上限：min(配置人数, 剩余人数);
    if (超出上限?) then (是)
        :禁止勾选，提示;
    else (否)
        :允许确认;
        :点击"确认本轮入围名单";
        :轮次状态 = "2-已完成";
        :组装本轮递补单位数据;
        :推送最后一轮递补数据给评标;
        :更新“是否可递补”标记;
    endif
else (否)
    :按钮置灰，等待有效选择;
endif

if (评标再次推送递补指令?) then (是)
    :返回新建轮次步骤;
else (否)
    :结束;
endif
stop
@enduml