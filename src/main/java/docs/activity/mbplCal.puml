@startuml
title 设置最高投标限价和评分流程

start

:检查是否设置最高投标限价;
if (是否设置最高投标限价？) then (是)
  :“最高限价发布策略”必填;
  if (“最高限价发布策略”是“随招标文件一起发布”?) then (是)
    :MBPL必填;
    if (MBPL有值?) then (有)
      :正常自动计算报价打分;
    else (无)
      :进入【手动算分】;
    endif
  else (“前N天发布”)
    :“最高限价发布截止天数”必填;
    :MBPL可填可不填;
    if (MBPL有值?) then (有)
      :正常自动计算报价打分;
    else (无)
      :进入【手动算分】;
    endif
  endif


   :检查是否因“前N天发布”未设置MBPL而进入【手动算分】;

   if (因“前N天发布”未设置MBPL导致【手动算分】?) then (是)
     :手动算分，但不再卡流;
   else (否)
     :执行原始手动算分逻辑，不卡流;
   endif

   else (否)
     :最高限价发布策略不填;
   endif
stop
@enduml
